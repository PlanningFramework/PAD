(define (domain domainName)
  (:requirements :typing :adl :fluents)
  (:constants constA constB constC)
  (:predicates (pred1) (pred2 ?a))
  (:functions (numericFunc ?a) - number
              (objectFunc) - object
  )
)
